import UIKit

final class PDFBuilder {
    /// Builds a searchable PDF with an image layer and an invisible text layer.
    func build(image: UIImage, receiptData: ReceiptData) async throws -> Data {
        throw ReceiptVaultError.notImplemented
    }
}
