import SwiftUI

extension Color {
    /// Primary brand teal, taken from the middle of the app icon gradient.
    static let brandPrimary = Color("BrandPrimary")

    /// Soft background tint used for cards and subtle surfaces.
    static let brandBackground = Color("BrandBackground")

    /// Accent teal for highlights if needed.
    static let brandAccent = Color("BrandAccent")
}

